// Re-export DonutStat from the manager component directory.
// Both portals share the same stateless donut chart primitive;
// the `accent` prop controls the role colour.
export { DonutStat } from '@/components/manager/DonutStat';
